server:
  port: 8080
spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://localhost:3306/skillrat_auth?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: skillrat
    password: skillrat
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  redis:
    host: localhost
    port: 6379
skillrat:
  auth:
    token-store: redis # redis|jdbc
  gateway:
    check-token-cache-ttl-seconds: 30
management:
  endpoints:
    web:
      exposure:
        include: "*"
